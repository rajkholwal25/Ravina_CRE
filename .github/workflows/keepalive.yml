name: Keep Render app awake

# Pings the app every ~10 minutes so Render's free tier doesn't spin down.
# After deploying, add a repository variable named RENDER_URL
#   (Settings → Secrets and variables → Actions → Variables → New variable)
#   e.g. RENDER_URL = https://dialysis-quiz.onrender.com
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          URL="${{ vars.RENDER_URL }}"
          if [ -z "$URL" ]; then
            echo "RENDER_URL variable not set — skipping. Add it in repo Variables."
            exit 0
          fi
          echo "Pinging $URL/healthz"
          code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 "$URL/healthz" || echo "000")
          echo "HTTP $code"
